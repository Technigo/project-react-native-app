import { getAppLoadingLifecycleEmitter } from 'expo/build/launch/AppLoading';
import React, { useState } from 'react';

import styled from 'styled-components/native'


// Small components that has its own structure, styling and logic. CSS breaks out the styling part.
// No conflicting classnames. Styled components is a library. We can use it in React, and React native. 
// styled-components.com
// 