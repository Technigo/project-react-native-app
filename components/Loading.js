import React from "react"
import { ActivityIndicator } from "react-native"

const Loading = () => {
  return (
    <ActivityIndicator
      size="large"
      color="#ff5447"
    />
  )
}

export default Loading